# Core dependencies — pinned to known-good versions (Issue 1.4)
playwright>=1.58.0,<2.0
pytest>=9.0.0,<10.0
pytest-playwright>=0.7.0,<1.0
pytest-bdd>=8.1.0,<9.0
pytest-html>=4.2.0,<5.0
pytest-xdist>=3.8.0,<4.0
allure-pytest>=2.15.0,<3.0

# Data handling
pyyaml>=6.0.0,<7.0
pandas>=2.2.0,<3.0
openpyxl>=3.1.0,<4.0

# Logging and utilities
python-dotenv>=1.2.0,<2.0

# Agentic AI + RAG
openai>=2.24.0,<3.0
qdrant-client>=1.17.0,<2.0

# Security & quality tooling (dev / CI)
pip-audit>=2.7.0        # Dependency vulnerability scanning
safety>=3.0.0           # Safety DB vulnerability checks
ruff>=0.3.0             # Linting (replaces flake8/isort/pyflakes)
pre-commit>=3.6.0       # Git hook management
detect-secrets>=1.4.0   # Secret leak prevention
